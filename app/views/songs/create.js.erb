$(document).ready(function(data) {
  
  $("#new_song").unbind("ajax:success").unbind("ajax:error").on("ajax:success", function(evt, data, status, xhr) {
    oQueue.update();
  }).on("ajax:error", function(xhr, status, error) {
    alert('error');
  });

});